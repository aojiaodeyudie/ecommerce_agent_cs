# 电商智能客服依赖（Python 3.10+）
# 安装：pip install -r requirements.txt
# 配置：复制 .env.example 为 .env 并填入 DASHSCOPE_API_KEY

# LangChain v1（create_agent / agents.middleware 新 API）
langchain>=1.0
langchain-core>=1.0
langchain-openai>=1.0
langchain-community>=1.0
langchain-chroma>=1.0
langgraph>=1.0

# 向量库 / 文档加载
chromadb>=1.0
pypdf>=5.0

# 配置 / HTTP
pyyaml>=6.0
requests>=2.32
python-dotenv>=1.0        # .env 配置加载（C1）
pytest>=8.0               # 测试（C2）

# API 服务（FastAPI 层）
fastapi>=0.115
uvicorn[standard]>=0.30
sse-starlette>=2.1        # SSE 流式对话
httpx>=0.27               # TestClient 依赖
